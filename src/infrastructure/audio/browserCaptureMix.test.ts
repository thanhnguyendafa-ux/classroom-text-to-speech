import assert from "node:assert/strict"; import test from "node:test"; import { createCaptureAudioMix } from "./browserCaptureMix";
test("connects available sources once and prefers the mixed destination track", () => { const connections: unknown[] = []; const mixedTrack = {}; const recorderTracks: unknown[] = []; const display = { getAudioTracks: () => [{}] } as MediaStream; const destination = { stream: { getAudioTracks: () => [mixedTrack] } }; const context = { createMediaStreamDestination: () => destination, createMediaStreamSource: () => ({ connect: (target: unknown) => connections.push(target) }) } as unknown as AudioContext; const original = globalThis.MediaStream; (globalThis as any).MediaStream = class { addTrack(track: unknown) { recorderTracks.push(track); } }; try { createCaptureAudioMix(context, display, null, {} as AnalyserNode); assert.equal(connections.length, 2); assert.deepEqual(recorderTracks, [mixedTrack]); } finally { (globalThis as any).MediaStream = original; } });
