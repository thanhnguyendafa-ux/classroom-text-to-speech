import assert from "node:assert/strict"; import test from "node:test"; import { encodeCapturedAudio } from "./browserAudioEncodingStrategy";
test("mixes stereo, reports clipping, and delegates encoding", () => { let captured: Int16Array | null = null; const result = encodeCapturedAudio({ length: 2, numberOfChannels: 2, sampleRate: 24000, duration: 1, getChannelData: channel => channel === 0 ? new Float32Array([1, .5]) : new Float32Array([1, -.5]) }, (pcm) => { captured = pcm; return new Blob([pcm]); }); assert.deepEqual([...(captured as unknown as Int16Array)], [32767, 0]); assert.equal(result.metrics.peak, 1); assert.equal(result.metrics.clippingRatio, .5); assert.equal(result.metrics.isLikelyClipped, true); });
