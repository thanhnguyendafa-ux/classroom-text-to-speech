import assert from "node:assert/strict"; import test from "node:test"; import { runAudioPreflight } from "./browserAudioPreflight";
test("detects signal and returns peak after deterministic timeout", async () => { let clock = 0; let callback: (() => void) | undefined; let cancelled = 0; const resultPromise = runAudioPreflight({ analyser: { frequencyBinCount: 2, getByteFrequencyData: data => { data[0] = 5; data[1] = 0; } }, speak: () => undefined, cancel: () => { cancelled += 1; }, now: () => clock, intervalMs: 100, timeoutMs: 200, setInterval: cb => { callback = cb; return 1 as any; }, clearInterval: () => undefined }); clock = 100; callback!(); clock = 200; callback!(); assert.deepEqual(await resultPromise, { detected: true, peak: 2.5 }); assert.equal(cancelled, 2); });

