import { X } from 'lucide-react';
import { AnimatePresence, motion } from 'motion/react';

export interface AppToastModel { type: 'success' | 'error' | 'info'; message: string; description?: string; action?: { label: string; onClick: () => void }; }
export function AppToast({ toast, onDismiss }: { toast: AppToastModel | null; onDismiss: () => void }) {
  return <AnimatePresence>{toast && <motion.div initial={{opacity:0,y:50,scale:0.95}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20,scale:0.95}} className="fixed bottom-6 right-6 z-[999] max-w-sm w-full bg-slate-900 border border-slate-800 text-white rounded-2xl shadow-xl p-4 flex gap-3.5 items-start font-sans"><div className="flex-1 space-y-1"><h4 className="text-xs font-extrabold flex items-center gap-1.5"><span className={`inline-block w-2.5 h-2.5 rounded-full shrink-0 ${toast.type === 'success' ? 'bg-emerald-500' : toast.type === 'error' ? 'bg-rose-500' : 'bg-sky-500'}`} />{toast.message}</h4>{toast.description && <p className="text-[11px] text-slate-400 font-medium leading-relaxed">{toast.description}</p>}</div><div className="flex flex-col items-end gap-2 shrink-0"><button type="button" onClick={onDismiss} className="text-slate-400 hover:text-white p-0.5 rounded-lg hover:bg-slate-800 transition cursor-pointer"><X className="w-3.5 h-3.5" /></button>{toast.action && <button type="button" onClick={() => { toast.action?.onClick(); onDismiss(); }} className="mt-1 text-[10px] font-bold bg-indigo-600 hover:bg-indigo-700 text-white px-2.5 py-1.5 rounded-lg transition active:scale-95 cursor-pointer shadow-sm">{toast.action.label}</button>}</div></motion.div>}</AnimatePresence>;
}
